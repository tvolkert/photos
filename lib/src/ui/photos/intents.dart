import 'package:flutter/widgets.dart';

class RewindIntent extends Intent {
  /// Creates a const [RewindIntent].
  const RewindIntent();
}

class FastForwardIntent extends Intent {
  /// Creates a const [FastForwardIntent].
  const FastForwardIntent();
}

class ZoomIntent extends Intent {
  /// Creates a const [ZoomIntent].
  const ZoomIntent();
}

class ShrinkIntent extends Intent {
  /// Creates a const [ShrinkIntent].
  const ShrinkIntent();
}

class SpinIntent extends Intent {
  /// Creates a const [SpinIntent]
  const SpinIntent();
}
