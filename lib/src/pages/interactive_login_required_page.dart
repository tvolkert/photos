import 'package:flutter/material.dart';

class InteractiveLoginRequiredPage extends StatelessWidget {
  const InteractiveLoginRequiredPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        // TODO: Make this look pretty
        child: Text('Interactive sign-in required'),
      ),
    );
  }
}
