name: photos
description: A Google Photos leanback experience
version: 1.0.0

environment:
  sdk: ">=2.3.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  file: ^6.1.2
  google_sign_in: ^5.1.1
  scoped_model: ^1.0.1
  http: ^0.12.0+1
  json_annotation: ^2.1.0
  path: any
  path_provider: any

dev_dependencies:
  flutter_test:
    sdk: flutter
  build_runner: ^1.0.0
  json_serializable: ^2.0.0

builders:
  json_serializable: ^2.0.0

flutter:
  uses-material-design: true
